<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Read</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-white p-10">
        <div class="w-full h-16 bg-white flex justify-between items-center border-b-2 border-gray-300 mb-2">
            <h1 class="text-5xl text-gray-500 tracking-tight"><span>Hello, </span><%= user.name %></h1>
            <div class="links flex justify-between items-center gap-7">
                <a href="/profile" class="text-xl text-gray-400">Write Posts</a>
                <a href="/logout" class="text-xl text-blue-500">Log Out</a>
            </div>
        </div>

        <div class="posts">
            <h2 class="text-gray-500 text-4xl mt-12 text-center">Catch up with the most recent posts</h2>
            <div class="postcontainer mt-7 w-full flex justify-center items-center flex-wrap">
                <% posts.reverse().forEach(function(post) { %>
                    <div class="post w-1/3 p-2 border-[1px] m-3 bg-gray-100 rounded-lg">
                        <h4 class=" text-gray-00">@<%= post.user.username %></h4>
                        <p class=" text-gray-500"><%= post.content %></p>
                        <small><%= post.likes.length %> likes</small>
                        <div class="buttons flex gap-4 mt-2">
                            <a class="text-blue-500" href="/likef/<%= post._id %>"><%= post.likes.includes(user._id) ? "Unlike" : "Like" %></a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
    <div class="footer w-full bg-gray-200 h-32 text-center text-zinc-600 p-10">
        <h2 class="mt-2">2024 CampusBuzz . All Rights Reserved.</h2>
    </div>
</body>
</html>
